# 文字認識仕様（Web版）

このドキュメントは、`app.js` に実装されている手書き数字認識ロジックの仕様をまとめたものです。

## 目的

- 幼児向け計算クイズで、手書き入力された数字をできるだけ安定して判定する
- 汎用OCRではなく、`0-9` の数字認識に特化して補正する

## 全体フロー

1. キャンバスに手書き入力
2. 描画停止後、短い待機時間（約450ms）で自動認識
3. 前処理画像を作成
4. 複数方式で認識候補を生成
5. 候補をスコアリングして最良候補を採用
6. 白いチップに認識結果を表示
7. `こたえる` で正誤判定

---

## 前処理

`createPreprocessedCanvas()` で以下を実施します。

- 手書き領域のバウンディングボックス抽出
- 余白をつけて切り出し
- 拡大（スケール3倍）
- 二値化（しきい値ベース）

目的:

- OCRやテンプレート照合で使いやすい形に統一する

---

## 候補生成（複数方式）

認識は1つの方法に依存せず、以下の候補を作成します。

1. `recognizeFromCanvas(preprocessed, 10)`  
   Tesseract（単一文字向け）
2. `recognizeFromCanvas(preprocessed, 7)`  
   Tesseract（単語行向け）
3. `recognizeByComponents(preprocessed)`  
   連結成分ごとに分割してOCR
4. `recognizeByTemplate(preprocessed)`  
   テンプレート照合（数字フォント行列比較）
5. `recognizeFromCanvas(originalCanvas, 10)`  
   元画像でのフォールバック

さらに形状条件を満たす場合、`1` の強制候補を追加します。

- 細長い
- 穴なし
- 連結成分が少ない

---

## 正規化

`normalizeDigitLikeText()` と `normalizeRecognizedDigits()` で文字を整形します。

- 例: `O/〇/o -> 0`, `I/| -> 1`
- `00 -> 0`
- `012 -> 12`

目的:

- OCR由来の揺れを数字として扱いやすくする

---

## 形状特徴

補助特徴として以下を使います。

- `countHoles()`  
  穴の数（例: 8は穴2個になりやすい）
- `getInkMetrics()`  
  縦横比、成分数、インク量

---

## 候補スコアリング

`scoreRecognitionCandidate()` で候補ごとにスコアを算出し、最大スコアを採用します。

主な補正内容:

- 穴数に応じた補正
  - 穴0個なら `0/6/8/9` を減点
  - 穴1個なら `0/6/9` を加点、`8` は減点
  - 穴2個以上なら `8` を加点
- `1` 専用補正
  - 細長い1本線は `1` を強く加点
  - `3/5/8/0/6/9` を減点
- 問題文脈補正
  - 問題の正解値（`state.currentQuestion.answer`）と一致する候補を加点
  - 1桁だけでなく、2桁以上の完全一致も加点

---

## 判定時の扱い

- 自動表示の認識と、`こたえる` 実行時の認識は同じロジックを使用
- 入力モードがテンキーの場合はテンキー値をそのまま使用
- 認識失敗時は `-` 表示で再入力

---

## 既知の限界

- 汎用OCRではないため、極端に崩れた字・接触した多桁は誤認識の可能性あり
- 端末差（解像度、タッチ精度）で認識率が変わる場合がある

---

## 調整ポイント

精度をさらに調整する場合は以下を優先します。

1. `createPreprocessedCanvas()` の二値化しきい値
2. `recognizeByTemplate()` の距離しきい値
3. `scoreRecognitionCandidate()` の加点・減点重み
4. `1` 以外（`4/7`, `3/5`）向けの形状ルール追加

